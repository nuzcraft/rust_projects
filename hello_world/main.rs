fn main() { // main is is always the first code run in every executable
    println!("Hello, world!");
    // println is a rust macro (as denoted by !)
    // this in indented by 4 spaces
    // most lines end with a ;
}